Statement以及PreparedStatement都能执行sql语句，而且PreparedStatement继承Statement；
但是使用PreparedStatement可以避免注入攻击，因为PreparedStatement能执行带？的sql语句，例如：select * from emps where userName=? and psaaWord=?
将此sql发送给DB后，DB会将其先编译成计划，DB在接受条件执行sql时，会保障计划的本意不变，即能避免了注入攻击
